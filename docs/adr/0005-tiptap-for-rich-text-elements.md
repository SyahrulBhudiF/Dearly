# Use TipTap for rich text Canvas Elements

Dearly uses TipTap/ProseMirror for rich text editing inside text Canvas Elements instead of building a custom contenteditable editor. This keeps the app Foldkit-first while relying on a mature editor core for selection, paste handling, inline styles, font controls, and mobile text editing behavior.
